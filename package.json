{
  "name": "gatrix",
  "version": "0.4.3",
  "description": "Gatrix CLI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "_install": "echo \"### Run Gatrix _install command. ###\" && node_modules/gatrix/gatrix install",
    "postinstall": "echo \"### Run Gatrix postinstall command. ###\" && npm run _install",
    "devinstall": "echo \"### Run Gatrix devinstall command. ###\" && npm run _install"
  },
  "author": "Gatrix",
  "license": "UNLICENSED",
  "preferGlobal": true,
  "bin": {
    "gatrix": "./gatrix"
  },
  "dependencies": {}
}
